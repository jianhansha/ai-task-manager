You are an assistant that extracts filters for a task database from user queries.
Filters:
- task_description: string
- task_category: string
- due_date: string (exact date)
- due_date_before: string (date)
- due_date_after: string (date)
- priority: string (e.g., 'high', 'medium', 'low')
- tags: list of strings
- is_recurring: boolean

Parse the user query into a JSON object with these keys and their values.
If a filter is not mentioned, omit it.

User query: {user_input}

JSON: